// Re-export from shared — single source of truth for all path constants
export {
  autoInstallGenome,
  BUNDLED_GENOMES_DIR,
  bundledGenomeDir,
  creatureDir,
  CREATURES_DIR,
  GENOMES_DIR,
  installedGenomeDir,
  OPENSEED_HOME,
  parseGenomeSource,
  requireGenomeDir,
  resolveGenomeDir,
} from '../shared/paths.js';
