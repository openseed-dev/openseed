# LLM API Keys (you need at least one)
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...

# Orchestrator port (default: 7770)
# Works for both native and Docker Compose.
# ORCHESTRATOR_PORT=7770

# Data directory on your HOST machine (required for Docker Compose).
# Must be an absolute path (don't use "~").
# macOS/Linux example:
# OPENSEED_HOME=/Users/you/.openseed
# Windows example:
# OPENSEED_HOME=C:/Users/you/.openseed

# Host path for creature bind mounts is derived from OPENSEED_HOME automatically.
